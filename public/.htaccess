Options -MultiViews
RewriteEngine On

# Serve Vite's client-side routes from index.html, while leaving real files alone.
RewriteCond %{REQUEST_FILENAME} -f [OR]
RewriteCond %{REQUEST_FILENAME} -d
RewriteRule ^ - [L]
RewriteRule ^ index.html [L]
